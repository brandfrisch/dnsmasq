---
dependency:
  name: galaxy
driver:
  name: docker
docker:
  containers:
    - name: dnsmasq-ubuntu-16-04
      image: brandfrisch/ubuntu-16.04
      image_version: 'latest'
      command: /lib/systemd/systemd
      privileged: true
    # - name: dnsmasq-centos-6
    #   image: centos
    #   image_version: 6
verifier:
  name: goss
