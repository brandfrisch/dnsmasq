---
dependency:
  name: galaxy
driver:
  name: docker
docker:
  containers:
    - name: dnsmasq
      image: brandfrisch/ubuntu-16.04
      image_version: 'latest'
      command: /lib/systemd/systemd
      privileged: true
verifier:
  name: goss
